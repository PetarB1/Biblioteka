@model IEnumerable<Biblioteka.Models.Autori>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Autori</h2>

@if (TempData["ErrorMessage"] != null)
{
    <div class="alert alert-danger">
        @TempData["ErrorMessage"]
    </div>
}

<p>
    @Html.ActionLink("Novi autor", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Ime)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Prezime)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Ime)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Prezime)
            </td>
            <td>
                @Html.ActionLink("Izmjeni", "Edit", new { id = item.Id, @class = "btn btn-primary" }) |
                @Html.ActionLink("Detalji", "Details", new { id = item.Id, @class = "btn btn-primary" }) |
                @Html.ActionLink("Izbriši", "Delete", new { id = item.Id, @class = "btn btn-primary" })
            </td>
        </tr>
    }

</table>
